# More Demo Tickets - Copy Title + Description Only

## Ticket #4: Data Export Missing Columns

**Title:**
```
CSV export not showing all required columns for audit
```

**Description:**
```
When I export our monthly report to CSV, several critical columns are missing from the output including "Last Updated", "Status", and "Audit Notes". The columns display correctly in the dashboard but don't appear in the exported file. I need these columns for our compliance audit which is due next week.
```

**Will match:**
- "Data export to CSV is missing columns" (96%)
- "Reports showing duplicate entries" (70%)

---

## Ticket #5: Two-Factor Authentication Broken

**Title:**
```
Cannot enable 2FA - QR code authentication fails
```

**Description:**
```
I'm trying to enable two-factor authentication on my account for security. After scanning the QR code with my authenticator app and entering the 6-digit code, I get an "Invalid token" error. I've tried with Google Authenticator and Microsoft Authenticator but both fail. This is blocking me from securing my account.
```

**Will match:**
- "Two-factor authentication not working" (95%)
- "Unable to reset password" (75%)

---

## Ticket #6: Bulk User Import Needed

**Title:**
```
Need feature to upload multiple users at once
```

**Description:**
```
Currently we add users one by one which is very time consuming. We hire 40-50 new employees each month and manually creating accounts takes hours. It would be a huge time-saver if we could upload a CSV file with user data to bulk create accounts in a single operation instead of going through the form 50 times.
```

**Will match:**
- "Request: Add bulk user import feature" (97%)
- "Request: API documentation needs improvement" (60%)

---

## Ticket #7: Duplicate Data in Reports

**Title:**
```
Monthly reports showing transactions twice
```

**Description:**
```
Our financial reports are showing duplicate transaction entries. When I sum up the total, it's nearly double what it should be because each transaction appears twice in the report. This is a serious issue for our accounting department and is affecting our month-end close process. We need this fixed urgently.
```

**Will match:**
- "Reports showing duplicate entries" (98%)
- "Data export to CSV is missing columns" (72%)

---

## Ticket #8: API Performance Issue

**Title:**
```
Users API endpoint timing out with large requests
```

**Description:**
```
The /api/users endpoint becomes very slow when querying more than 10,000 records. Response times are 6-8 seconds instead of the expected sub-500ms. Our third-party integration is timing out because of this delay. This is impacting our data synchronization pipeline and is starting to affect production.
```

**Will match:**
- "API response time exceeds SLA" (94%)
- "Dashboard loading slowly during peak hours" (78%)

---

## Ticket #9: Dark Mode Request

**Title:**
```
Need dark mode for night shift staff
```

**Description:**
```
Our support team works 24/7 including night shifts. The bright white interface is causing eye strain and fatigue during late night hours. Other tools we use have dark mode which helps significantly. Could you add a dark mode toggle to the application? This would make overnight shifts much more comfortable for our team.
```

**Will match:**
- "Request: Dark mode support" (95%)
- "Request: API documentation needs improvement" (65%)

---

## Ticket #10: Memory Error on Large Export

**Title:**
```
Export fails with out of memory error for large datasets
```

**Description:**
```
When I try to export more than 50,000 records, the system throws an "Out of Memory" error and the export fails completely. I need to export our full dataset of 150,000+ records for analysis but currently cannot do this. Could the export handle large datasets better, perhaps by exporting in chunks or using pagination?
```

**Will match:**
- "Cannot export data due to memory error" (96%)
- "Data export to CSV is missing columns" (75%)
- "API response time exceeds SLA" (70%)

---

## Quick Paste Format

Just copy **Title** and **Description** separately into the form fields.

**Demo flow:** Create tickets 4, 5, 6 to show variety:
- ✅ **#4** = Data accuracy issue
- ✅ **#5** = Security/authentication issue  
- ✅ **#6** = Feature request

Then show how each finds similar resolved tickets from different categories.
